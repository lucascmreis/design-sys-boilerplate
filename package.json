{
  "name": "design-system-template",
  "version": "1.0.0",
  "description": "- Split in packages",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "typescript": "^4.9.3"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
